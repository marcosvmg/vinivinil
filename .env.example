APP_NAME=Vinivinil
APP_ENV=production
APP_KEY= # (Render vai gerar se você rodar `php artisan key:generate`)
APP_DEBUG=false
APP_URL=https://vinivinil.onrender.com

APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=pt_BR

# Conexão com banco PostgreSQL (padrão no Render)
DB_CONNECTION=pgsql
DB_HOST=your-db-host.render.com     # <- Substitua pelo host exato do Render
DB_PORT=5432
DB_DATABASE=vinivinil               # <- Nome do banco criado
DB_USERNAME=vinivinil_user          # <- Nome do usuário criado
DB_PASSWORD=w1hRNUWkkIiFgrKdjDFkWK0yQQRtBEhR

# Sessões e cache (usar banco evita problemas com múltiplos containers)
SESSION_DRIVER=database
SESSION_LIFETIME=120
CACHE_DRIVER=database
QUEUE_CONNECTION=sync

# LOG
LOG_CHANNEL=stderr
LOG_LEVEL=info

# E-mail (ajuste conforme necessário ou deixe como log para testes)
MAIL_MAILER=log
MAIL_FROM_ADDRESS=no-reply@vinivinil.onrender.com
MAIL_FROM_NAME="${APP_NAME}"

# Arquivos públicos
FILESYSTEM_DISK=public
